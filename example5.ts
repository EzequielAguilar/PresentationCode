// Enum allows you to group values together with friendlier names
  enum ListOfBirds {
    Finch,
    Budgie,
    Parrot,
    Eagel,
    Duck
  }

  console.log(ListOfBirds);

  let whois2 = ListOfBirds[2];
  console.log(whois2);